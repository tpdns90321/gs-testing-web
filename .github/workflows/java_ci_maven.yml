name: Java CI with Maven

on:
  push:
    branches: [ main, feature/build_ci ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Install dependencies
      run: mvn install -DskipTests
    - name: Compile
      run: mvn compile
    - name: Scan with PMD
      run: mvn pmd:check
    - name: Scan with SpotBugs
      run: mvn spotbugs:check
    - name: Scan with CheckStyle
      run: mvn checkstyle:check
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Install dependencies
      run: mvn install -DskipTests
    - name: Compile
      run: mvn compile
    - name: Run tests
      run: mvn test
